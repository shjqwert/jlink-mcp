# JScope Headless CLI Discovery

Conclusion: `JSCOPE_HEADLESS_EXPORT_MISSING`.

Commands tried:

- `JScope.exe --help`
- `JScope.exe -?`
- `JScope.exe /?`

All three attempts produced no stdout/stderr within the 5 second timeout and were terminated. No official headless start/stop/export CLI evidence was found in local install text files. GUI preflight remains `preflight_only`, not a benchmark.
