# Historical documents

Documents in this directory describe earlier HSS MVP and approval-era implementations. They are retained only as historical migration evidence and are not current interfaces, requirements, commands, or acceptance results.

The authoritative current change is `openspec/changes/refactor-agent-first-mcp/`.
