# UI Baseline

`jlink-mcp-offline-analysis-wireframe.png` is the accepted MVP interaction baseline.

Normative details are defined in `design.md` and `specs/offline-analysis-ui/spec.md`; the image is a layout reference, not a pixel-perfect visual specification.
